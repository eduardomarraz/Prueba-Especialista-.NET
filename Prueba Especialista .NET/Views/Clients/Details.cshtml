@model Prueba_Especialista_.NET.Models.Client

@{
    ViewData["Title"] = "Detalles del Cliente";
}

<h1>Detalles del Cliente</h1>
<hr />
<dl class="row">
    <dt class="col-sm-2">@Html.DisplayNameFor(m => m.Name)</dt>
    <dd class="col-sm-10">@Html.DisplayFor(m => m.Name)</dd>

    <dt class="col-sm-2">@Html.DisplayNameFor(m => m.Address)</dt>
    <dd class="col-sm-10">@Html.DisplayFor(m => m.Address)</dd>
</dl>

<a asp-action="Index" class="btn btn-secondary">Volver</a>
